# 📦 MinIO - Хранилище изображений

## Что такое MinIO?

MinIO - это S3-совместимое объектное хранилище, которое работает локально в Docker контейнере. Это отличная альтернатива AWS S3 для разработки и даже для продакшена.

## ✅ Автоматическая настройка

MinIO **автоматически** запускается при использовании `docker compose up`!

### Что происходит автоматически:

1. ✅ MinIO контейнер запускается
2. ✅ Bucket `bazaar-images` создается автоматически
3. ✅ Backend подключается к MinIO
4. ✅ Готово к использованию!

## 🔧 Ручная настройка (если нужно)

### 1. Откройте MinIO Console

После запуска контейнеров откройте: **http://localhost:9001**

**Учетные данные:**
- Username: `minioadmin`
- Password: `minioadmin`

### 2. Настройте публичный доступ

1. В MinIO Console откройте bucket `bazaar-images`
2. Перейдите в **"Access Policy"** (слева в меню)
3. Выберите **"Public"** для чтения файлов
4. Сохраните

**Важно:** Без этого шага изображения не будут доступны для просмотра!

## 🚀 Использование

После настройки:

1. Создайте объявление
2. Нажмите **"Загрузить изображение"**
3. Выберите файл
4. Изображение загрузится в MinIO
5. URL будет автоматически добавлен в форму

## 📍 Где хранятся файлы?

- **В Docker volume**: `minio_data`
- **URL изображений**: `http://localhost:9000/bazaar-images/items/...`

## 🔐 Изменение пароля (для продакшена)

В `docker-compose.yml` измените:

```yaml
minio:
  environment:
    MINIO_ROOT_USER: ваш-username
    MINIO_ROOT_PASSWORD: ваш-надежный-пароль
```

И обновите в `backend` сервисе:
```yaml
AWS_ACCESS_KEY_ID: ваш-username
AWS_SECRET_ACCESS_KEY: ваш-надежный-пароль
```

## 🐛 Решение проблем

### Bucket не создается автоматически

1. Зайдите в MinIO Console (http://localhost:9001)
2. Создайте bucket `bazaar-images` вручную
3. Настройте публичный доступ

### Изображения не отображаются

1. Проверьте, что bucket имеет публичный доступ
2. Проверьте URL изображения в консоли браузера
3. Убедитесь, что MinIO запущен: `docker compose ps`

### Ошибка подключения

1. Проверьте, что MinIO контейнер запущен
2. Проверьте логи: `docker compose logs minio`
3. Убедитесь, что backend ждет запуска MinIO (healthcheck)

## 📚 Дополнительная информация

- **MinIO Console**: http://localhost:9001
- **MinIO API**: http://localhost:9000
- **Документация MinIO**: https://min.io/docs





