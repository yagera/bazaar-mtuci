# 🚀 Быстрый старт Bazaar MTUCI

## Самый простой способ

### 1. Клонируйте репозиторий (если еще не сделали)
```bash
git clone <repository-url>
cd bazaar-mtuci
```

### 2. Запустите одной командой
```bash
./start.sh
```

**Готово!** 🎉

Проект автоматически:
- Установит все зависимости
- Настроит окружение
- Создаст конфигурационные файлы
- Запустит оба сервера

Откройте в браузере:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

---

## Альтернативные способы

### Через Make
```bash
make install  # Первый раз
make start     # Запуск
```

### Через Docker
```bash
make docker-start
# или
docker-compose up
```

---

## Требования

- Python 3.11+
- Node.js 18+
- PostgreSQL (или используйте Docker версию)
- npm или yarn

---

## Остановка

Нажмите `Ctrl+C` в терминале или выполните:
```bash
make stop
```

---

## Проблемы?

1. **PostgreSQL не запущен**: Убедитесь, что PostgreSQL запущен, или используйте Docker версию
2. **Порт занят**: Измените порты в конфигурации
3. **Ошибки зависимостей**: Выполните `make clean && make install`

Для подробной документации см. [README.md](README.md)





