# Bazaar MTUCI

Сервис аренды вещей для жителей общежития МТУСИ. Платформа позволяет студентам сдавать в аренду свои вещи и бронировать вещи других студентов.

## 🎨 Дизайн

Современный дизайн в стиле 2025 года с фиолетовой цветовой схемой (#372579) и плавными анимациями.

## Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **Alembic** - миграции базы данных
- **JWT** - аутентификация и авторизация
- **Pydantic** - валидация данных
- **boto3** - интеграция с AWS S3 для загрузки изображений
- **bcrypt** - хеширование паролей

### Frontend
- **Next.js 14** - React фреймворк с SSR
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Framer Motion** - анимации
- **React Query** - управление состоянием сервера
- **React Hook Form** - работа с формами

## 🚀 Быстрый старт

### Запуск одной командой

```bash
./start.sh
# или
make start
# или через Docker
make docker-start
```

## 📦 Хранилище изображений

Проект использует **MinIO** (S3-совместимое хранилище) для загрузки изображений.

### Автоматическая настройка

MinIO автоматически запускается в Docker контейнере при использовании `docker-compose up`.

### Доступ к MinIO Console

- **URL**: http://localhost:9001
- **Username**: `minioadmin`
- **Password**: `minioadmin`

### Настройка публичного доступа

1. Откройте MinIO Console (http://localhost:9001)
2. Откройте bucket `bazaar-images`
3. Перейдите в **"Access Policy"**
4. Выберите **"Public"** для чтения

### Использование AWS S3 (опционально)

Если хотите использовать AWS S3 вместо MinIO, добавьте в `backend/.env`:

```env
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name
# Не указывайте AWS_S3_ENDPOINT_URL для AWS S3
```

Подробнее см. [MINIO_SETUP.md](MINIO_SETUP.md)

## Функционал

- ✅ Регистрация и авторизация (JWT)
- ✅ Создание объявлений с загрузкой изображений (S3 или URL)
- ✅ Бронирование вещей на определенное время
- ✅ Поиск по объявлениям
- ✅ Управление объявлениями
- ✅ Профиль пользователя
- ✅ Интеграция с Telegram

## API Endpoints

- `POST /api/v1/upload/image` - загрузка изображения в S3
- `POST /api/v1/auth/register` - регистрация
- `POST /api/v1/auth/login` - вход
- `GET /api/v1/items` - список объявлений
- `POST /api/v1/items` - создать объявление
- `POST /api/v1/bookings` - создать бронирование

## Лицензия

MIT
